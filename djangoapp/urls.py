from django.conf.urls import patterns, include, url
from .views import *

urlpatterns = patterns('',
    # Example:
    # url(r'^$', my_view, name='home'),
    # url(r'^stuff/$', MyClassView.as_view(), name='stuff'),
)
